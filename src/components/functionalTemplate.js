// @flow
import React from 'react';
import { connect } from 'react-redux';
import { graphql, compose } from 'react-apollo';

type Store = {

}

function mapToState({}: Store) {
	return {

	};
}

type Props = {

};

const Temp = (props: Props) => <div />;

export default connect(mapToState)(Temp);
